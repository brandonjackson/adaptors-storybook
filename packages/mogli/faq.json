{
  "generatedAt": "2026-05-14",
  "source": "tools/generate-faqs.mjs",
  "what": "OpenFn adaptor for Mogli SMS. The adaptor lets OpenFn workflows read from and write to Mogli SMS (Salesforce-native SMS app).\n\nOperations haven't been parsed for this adaptor yet — see the Sources tab for vendor API docs. Vendor homepage: https://mogli.com.",
  "connect": "Mogli Technologies supports OAuth 2.0 for API access.\n\nThe credential schema asks for loginUrl (required), username (required), password (required), securityToken (required), and secret (required). Auth reference: https://help.salesforce.com/s/articleView?id=sf.remoteaccess_oauth_flows.htm.",
  "when": "Reach for this adaptor when your workflow needs to read or write data in Mogli SMS (Salesforce-native SMS app).\n\nTriggers haven't been catalogued yet — drive the workflow from a cron schedule or an upstream webhook in the meantime. Mogli Technologies doesn't appear to expose webhooks, so plan on polling at a sensible interval.",
  "how": "In a job step, import the adaptor and chain its operations. Each operation reads and writes to job state, so the response from one becomes the input to the next:\n\nimport { operation } from \"@openfn/language-mogli\";\n\noperation({ /* params */ });\n\nSnippets haven't been seeded for this adaptor yet; the Operations tab shows each function's signature and JSDoc examples.",
  "wire": "A typical wiring pattern is trigger → fetch → transform → write.\n\nDrive the workflow from a cron schedule or an upstream webhook trigger. Then chain operations from this adaptor's Operations tab to fetch or write data in Mogli Technologies. Pass values between steps through state.data — every operation reads from and writes to job state.\n\nTo move data between systems, follow this step with another adaptor (e.g. write the fetched records into a downstream FHIR, DHIS2, or database adaptor).",
  "scale": "When running Mogli Technologies jobs at volume:\n\n- Mogli Technologies doesn't surface webhooks, so poll on the longest interval your SLA allows and use a since-cursor to avoid re-reading everything.\n- Batch requests where the API supports it and avoid fan-out loops over large collections.\n- Make jobs idempotent by keying on stable external IDs so a replay doesn't create duplicates.\n- Surface 4xx/5xx responses with the vendor request ID in your logs, and treat 429s as retries with backoff.\n\nAdapter note: Mogli is a Salesforce-managed package; adapter is effectively a thin wrapper over Salesforce REST/Bulk APIs (see salesforce adapter).",
  "best": "- Store Mogli Technologies credentials in OpenFn's credential store; never inline keys or tokens in job code.\n- Pin @openfn/language-mogli to v0.6.14 in package.json so an upstream change doesn't break a production workflow silently.\n- Keep each step small — one logical API call per operation makes failures easier to diagnose and retry.\n- Add defensive checks before transforming inbound payloads — vendor responses change between versions.\n- Log enough context (resource id, run id, vendor request id) so an incident can be traced end-to-end."
}
