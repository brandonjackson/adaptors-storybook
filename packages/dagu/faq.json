{
  "generatedAt": "2026-05-14",
  "source": "tools/generate-faqs.mjs",
  "what": "OpenFn dagu adaptor. The adaptor lets OpenFn workflows read from and write to Dagu Workflow Engine.\n\nIt exposes 3 operations that you can call from a workflow step, listed in the Operations tab. Vendor homepage: https://dagu.readthedocs.io.",
  "connect": "Dagu supports HTTP Basic auth for API access.\n\nThe credential schema asks for baseUrl (required), username (required), and password (required). Auth reference: https://dagu.readthedocs.io/en/latest/config/server.html.",
  "when": "Reach for this adaptor when your workflow needs to read or write data in Dagu Workflow Engine.\n\nTriggers haven't been catalogued yet — drive the workflow from a cron schedule or an upstream webhook in the meantime. Dagu doesn't appear to expose webhooks, so plan on polling at a sensible interval.",
  "how": "In a job step, import the adaptor and chain its operations. Each operation reads and writes to job state, so the response from one becomes the input to the next:\n\nimport { get, post, request } from \"@openfn/language-dagu\";\n\nget({ /* params */ });\n\nSnippets haven't been seeded for this adaptor yet; the Operations tab shows each function's signature and JSDoc examples.",
  "wire": "A typical wiring pattern is trigger → fetch → transform → write.\n\nDrive the workflow from a cron schedule or an upstream webhook trigger. Then chain operations from this adaptor's Operations tab to fetch or write data in Dagu. Pass values between steps through state.data — every operation reads from and writes to job state.\n\nTo move data between systems, follow this step with another adaptor (e.g. write the fetched records into a downstream FHIR, DHIS2, or database adaptor).",
  "scale": "When running Dagu jobs at volume:\n\n- Dagu doesn't surface webhooks, so poll on the longest interval your SLA allows and use a since-cursor to avoid re-reading everything.\n- Paginate list and search calls explicitly; never assume a single response is the full result set.\n- Make jobs idempotent by keying on stable external IDs so a replay doesn't create duplicates.\n- Surface 4xx/5xx responses with the vendor request ID in your logs, and treat 429s as retries with backoff.\n\nAdapter note: Self-hosted workflow scheduler; no canonical baseUrl — users supply their own instance URL.",
  "best": "- Store Dagu credentials in OpenFn's credential store; never inline keys or tokens in job code.\n- Pin @openfn/language-dagu to v1.2.1 in package.json so an upstream change doesn't break a production workflow silently.\n- Keep each step small — one logical API call per operation makes failures easier to diagnose and retry.\n- Add defensive checks before transforming inbound payloads — vendor responses change between versions.\n- Log enough context (resource id, run id, vendor request id) so an incident can be traced end-to-end."
}
