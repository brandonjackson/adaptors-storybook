{
  "generatedAt": "2026-05-14",
  "source": "tools/generate-faqs.mjs",
  "what": "OpenFn adaptor for Azure Storage. The adaptor lets OpenFn workflows read from and write to Azure Storage (Blob/Queue).\n\nIt exposes 3 operations that you can call from a workflow step, listed in the Operations tab. Vendor homepage: https://azure.microsoft.com/en-us/products/storage/blobs.",
  "connect": "Microsoft supports hmac, OAuth 2.0, or shared access signatures for API access.\n\nThe credential schema asks for accountName (required), accountKey (required), and containerName. Point the credential at the base URL that matches your environment: https://<account>.blob.core.windows.net, https://<account>.queue.core.windows.net. Auth reference: https://learn.microsoft.com/en-us/rest/api/storageservices/authorize-requests-to-azure-storage.",
  "when": "Reach for this adaptor when your workflow needs to read or write data in Azure Storage (Blob/Queue).\n\nTriggers haven't been catalogued yet — drive the workflow from a cron schedule or an upstream webhook in the meantime. Microsoft can push events to OpenFn via webhooks (docs: https://learn.microsoft.com/en-us/azure/storage/blobs/storage-blob-event-overview), so reactive workflows are a good fit.",
  "how": "In a job step, import the adaptor and chain its operations. Each operation reads and writes to job state, so the response from one becomes the input to the next:\n\nimport { uploadBlob, downloadBlob, getBlobProperties } from \"@openfn/language-azure-storage\";\n\ngetBlobProperties({ /* params */ });\n\nSnippets haven't been seeded for this adaptor yet; the Operations tab shows each function's signature and JSDoc examples.",
  "wire": "A typical wiring pattern is trigger → fetch → transform → write.\n\nDrive the workflow from a cron schedule or an upstream webhook trigger. Then chain operations from this adaptor's Operations tab to fetch or write data in Microsoft. Pass values between steps through state.data — every operation reads from and writes to job state.\n\nTo move data between systems, follow this step with another adaptor (e.g. write the fetched records into a downstream FHIR, DHIS2, or database adaptor).",
  "scale": "When running Microsoft jobs at volume:\n\n- Prefer Microsoft webhooks over polling — they cut latency and rate-limit pressure.\n- Batch requests where the API supports it and avoid fan-out loops over large collections.\n- Make jobs idempotent by keying on stable external IDs so a replay doesn't create duplicates.\n- Surface 4xx/5xx responses with the vendor request ID in your logs, and treat 429s as retries with backoff.\n\nAdapter note: REST-based but most integrations use the Azure SDK; auth via shared key, SAS or Azure AD.",
  "best": "- Store Microsoft credentials in OpenFn's credential store; never inline keys or tokens in job code.\n- Pin @openfn/language-azure-storage to v3.0.10 in package.json so an upstream change doesn't break a production workflow silently.\n- Keep each step small — one logical API call per operation makes failures easier to diagnose and retry.\n- Add defensive checks before transforming inbound payloads — vendor responses change between versions.\n- Log enough context (resource id, run id, vendor request id) so an incident can be traced end-to-end."
}
