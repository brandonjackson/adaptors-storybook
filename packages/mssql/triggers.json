{
  "source": "zapier",
  "scrapedAt": "2026-05",
  "notes": "Pure polling against the database — no native push channel. In OpenFn, use a cron schedule and a since-cursor column (auto-increment id, rowversion, updated_at).",
  "triggers": [
    {
      "name": "New Column",
      "description": "Fires when a new column is added.",
      "mechanism": "polling",
      "openfnPattern": "cron",
      "output": {
        "summary": "Column metadata (INFORMATION_SCHEMA.COLUMNS)",
        "fields": ["TABLE_SCHEMA", "TABLE_NAME", "COLUMN_NAME", "DATA_TYPE", "IS_NULLABLE"]
      }
    },
    {
      "name": "New or Updated Row",
      "description": "Fires when a new row is added or modified.",
      "mechanism": "polling",
      "openfnPattern": "cron",
      "output": {
        "summary": "Row from the watched table (schema-defined columns)",
        "fields": []
      }
    },
    {
      "name": "New Row",
      "description": "Fires when a new row is added.",
      "mechanism": "polling",
      "openfnPattern": "cron",
      "output": {
        "summary": "Row from the watched table (schema-defined columns)",
        "fields": []
      }
    },
    {
      "name": "New Row (Custom Query)",
      "description": "Fires when new rows are returned from a custom query.",
      "mechanism": "polling",
      "openfnPattern": "cron",
      "output": {
        "summary": "Row shape determined by the SELECT statement",
        "fields": []
      }
    },
    {
      "name": "New Table",
      "description": "Fires when a new table is added.",
      "mechanism": "polling",
      "openfnPattern": "cron",
      "output": {
        "summary": "Table metadata (INFORMATION_SCHEMA.TABLES)",
        "fields": ["TABLE_CATALOG", "TABLE_SCHEMA", "TABLE_NAME", "TABLE_TYPE"]
      }
    }
  ]
}
