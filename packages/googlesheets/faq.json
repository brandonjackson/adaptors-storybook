{
  "generatedAt": "2026-05-14",
  "source": "tools/generate-faqs.mjs",
  "what": "A Google Sheets Language Pack for OpenFn. The adaptor lets OpenFn workflows read from and write to Google Sheets API.\n\nIt exposes 3 operations that you can call from a workflow step, listed in the Operations tab. 28 ready-to-paste snippets are grouped as create (21) and search (7). Vendor homepage: https://developers.google.com/sheets/api.",
  "connect": "Google supports OAuth 2.0 or service-account for API access.\n\nThe credential schema asks for access_token (required). Point the credential at the base URL: https://sheets.googleapis.com. Auth reference: https://developers.google.com/sheets/api/guides/authorizing.",
  "when": "Reach for this adaptor when your workflow needs to read or write data in Google Sheets API.\n\n4 triggers are catalogued (0 webhook, 4 polling), including New or Updated Spreadsheet Row, New Spreadsheet, New Spreadsheet Row, New Worksheet. See the Triggers tab for the full list. Google doesn't appear to expose webhooks, so plan on polling at a sensible interval.",
  "how": "In a job step, import the adaptor and chain its operations. Each operation reads and writes to job state, so the response from one becomes the input to the next:\n\nimport { appendValues, batchUpdateValues, getValues } from \"@openfn/language-googlesheets\";\n\ngetValues({ /* params */ });\n\nThe Snippets tab has 28 ready-to-paste examples grouped by category — start there rather than from scratch.",
  "wire": "A typical wiring pattern is trigger → fetch → transform → write.\n\nKick the workflow off from a trigger on the Triggers tab (polled on a cron). Then chain operations from this adaptor's Operations tab to fetch or write data in Google. Pass values between steps through state.data — every operation reads from and writes to job state.\n\nTo move data between systems, follow this step with another adaptor (e.g. write the fetched records into a downstream FHIR, DHIS2, or database adaptor).",
  "scale": "When running Google jobs at volume:\n\n- Google doesn't surface webhooks, so poll on the longest interval your SLA allows and use a since-cursor to avoid re-reading everything.\n- Paginate list and search calls explicitly; never assume a single response is the full result set.\n- Make jobs idempotent by keying on stable external IDs so a replay doesn't create duplicates.\n- Surface 4xx/5xx responses with the vendor request ID in your logs, and treat 429s as retries with backoff.\n\nAdapter note: No native webhooks; use Drive API push notifications on the spreadsheet file for change events.",
  "best": "- Store Google credentials in OpenFn's credential store; never inline keys or tokens in job code.\n- Pin @openfn/language-googlesheets to v4.0.12 in package.json so an upstream change doesn't break a production workflow silently.\n- Keep each step small — one logical API call per operation makes failures easier to diagnose and retry.\n- Add defensive checks before transforming inbound payloads — vendor responses change between versions.\n- Log enough context (resource id, run id, vendor request id) so an incident can be traced end-to-end."
}
