{
  "generatedAt": "2026-05-14",
  "source": "tools/generate-faqs.mjs",
  "what": "An HTTP request language package for use with Open Function. The adaptor lets OpenFn workflows read from and write to HTTP client.\n\nIt exposes 7 operations that you can call from a workflow step, listed in the Operations tab. 4 ready-to-paste snippets are grouped as create (3) and search (1). Vendor homepage: https://datatracker.ietf.org/doc/html/rfc9110.",
  "connect": "IETF / generic supports HTTP Basic auth, bearer tokens, OAuth 2.0, or API keys for API access.\n\nThe credential schema asks for username, password, access_token, baseUrl, and tls. Auth reference: https://datatracker.ietf.org/doc/html/rfc7235.",
  "when": "Reach for this adaptor when your workflow needs to read or write data in HTTP client.\n\n3 triggers are catalogued (2 webhook, 1 polling), including Catch Hook, Catch Raw Hook, Retrieve Poll. See the Triggers tab for the full list. IETF / generic doesn't appear to expose webhooks, so plan on polling at a sensible interval.",
  "how": "In a job step, import the adaptor and chain its operations. Each operation reads and writes to job state, so the response from one becomes the input to the next:\n\nimport { request, get, post } from \"@openfn/language-http\";\n\nget({ /* params */ });\n\nThe Snippets tab has 4 ready-to-paste examples grouped by category — start there rather than from scratch.",
  "wire": "A typical wiring pattern is trigger → fetch → transform → write.\n\nKick the workflow off from a trigger on the Triggers tab (polled on a cron). Then chain operations from this adaptor's Operations tab to fetch or write data in IETF / generic. Pass values between steps through state.data — every operation reads from and writes to job state.\n\nTo move data between systems, follow this step with another adaptor (e.g. write the fetched records into a downstream FHIR, DHIS2, or database adaptor).",
  "scale": "When running IETF / generic jobs at volume:\n\n- IETF / generic doesn't surface webhooks, so poll on the longest interval your SLA allows and use a since-cursor to avoid re-reading everything.\n- Batch requests where the API supports it and avoid fan-out loops over large collections.\n- Make jobs idempotent by keying on stable external IDs so a replay doesn't create duplicates.\n- Surface 4xx/5xx responses with the vendor request ID in your logs, and treat 429s as retries with backoff.\n\nAdapter note: Generic protocol adapter — no vendor API. Exposes a thin, jobs-friendly wrapper around axios for arbitrary HTTP requests.",
  "best": "- Store IETF / generic credentials in OpenFn's credential store; never inline keys or tokens in job code.\n- Pin @openfn/language-http to v7.2.10 in package.json so an upstream change doesn't break a production workflow silently.\n- Keep each step small — one logical API call per operation makes failures easier to diagnose and retry.\n- Add defensive checks before transforming inbound payloads — vendor responses change between versions.\n- Log enough context (resource id, run id, vendor request id) so an incident can be traced end-to-end."
}
