{
  "generatedAt": "2026-05-14",
  "source": "tools/generate-faqs.mjs",
  "what": "OpenFn adaptor for accessing the OpenFn web API. The adaptor lets OpenFn workflows read from and write to OpenFn Platform API (Lightning / v1).\n\nIt exposes 3 operations that you can call from a workflow step, listed in the Operations tab. Vendor homepage: https://www.openfn.org.",
  "connect": "OpenFn supports API keys, bearer tokens, or HTTP Basic auth for API access.\n\nThe credential schema asks for baseUrl and access_token (required). Point the credential at the base URL that matches your environment: https://app.openfn.org/api, https://app.openfn.org. Auth reference: https://docs.openfn.org/documentation/api-tokens.",
  "when": "Reach for this adaptor when your workflow needs to read or write data in OpenFn Platform API (Lightning / v1).\n\nTriggers haven't been catalogued yet — drive the workflow from a cron schedule or an upstream webhook in the meantime. OpenFn can push events to OpenFn via webhooks (docs: https://docs.openfn.org/documentation/build/triggers), so reactive workflows are a good fit.",
  "how": "In a job step, import the adaptor and chain its operations. Each operation reads and writes to job state, so the response from one becomes the input to the next:\n\nimport { request, get, post } from \"@openfn/language-openfn\";\n\nget({ /* params */ });\n\nSnippets haven't been seeded for this adaptor yet; the Operations tab shows each function's signature and JSDoc examples.",
  "wire": "A typical wiring pattern is trigger → fetch → transform → write.\n\nDrive the workflow from a cron schedule or an upstream webhook trigger. Then chain operations from this adaptor's Operations tab to fetch or write data in OpenFn. Pass values between steps through state.data — every operation reads from and writes to job state.\n\nTo move data between systems, follow this step with another adaptor (e.g. write the fetched records into a downstream FHIR, DHIS2, or database adaptor).",
  "scale": "When running OpenFn jobs at volume:\n\n- Prefer OpenFn webhooks over polling — they cut latency and rate-limit pressure.\n- Paginate list and search calls explicitly; never assume a single response is the full result set.\n- Make jobs idempotent by keying on stable external IDs so a replay doesn't create duplicates.\n- Surface 4xx/5xx responses with the vendor request ID in your logs, and treat 429s as retries with backoff.\n\nAdapter note: Adapter targets OpenFn's own platform v1 REST API. No published OpenAPI spec yet; Lightning's Phoenix routes are the canonical source for the v2 surface.",
  "best": "- Store OpenFn credentials in OpenFn's credential store; never inline keys or tokens in job code.\n- Pin @openfn/language-openfn to v3.0.13 in package.json so an upstream change doesn't break a production workflow silently.\n- Keep each step small — one logical API call per operation makes failures easier to diagnose and retry.\n- Add defensive checks before transforming inbound payloads — vendor responses change between versions.\n- Log enough context (resource id, run id, vendor request id) so an incident can be traced end-to-end."
}
