{
  "name": "adaptor-library",
  "version": "0.0.1",
  "description": "Storybook-style browser for OpenFn adapters: schemas, operations, credentials, snippets, triggers.",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix ui install",
    "dev": "npm --prefix ui run dev",
    "build": "npm --prefix ui run build",
    "manifest": "npm --prefix ui run manifest"
  },
  "license": "LGPL-3.0"
}
